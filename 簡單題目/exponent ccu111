#include <stdio.h>

int exp(int b, int e)
{
	int result = 1;
	for (int i = 0; i < e; i++)
	{
		result *= b;
	}
	return result;
}

int main() {
	int b, e;
	scanf("%d %d", &b, &e);
    printf("%d", exp(b, e));
    return 0;
}
